const express = require('express');
const router = express.Router();
const urlController = require('../controllers/urlController');

// Vous pouvez ajouter des routes supplémentaires liées aux URLs ici

module.exports = router;
